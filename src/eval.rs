use crate::parser::{Node};
use crate::lexer::{Symb};


// use indirection to make more flexible.

// indirection is like a joint, it adds a degree of freedom in the
// design




// ------------------------------------------------------------------
/*
struct Eval {
    nodeMap: IdNodeMap,
    global: Env,
}

impl Eval {
    fn new() -> Eval {
        Eval{ node_map: IdNodeMap::new(),
              global: Env::new() }
    }

    fn lookup_variable_value(var: Symb, env: Env) {
    }
    
    // fn eval(exp: Expr, mut env: Env) { 
        
    // }

    
}

// fn eval(exp: Expr, env: Env) {
//     if exp.is_self_evaluating() {
//         exp
//     } else if exp.is_variable() {
//         env.lookup_variable(exp) 
//     }
// }


*/
